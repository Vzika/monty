#include "monty.h"
/**
 * stack_func -sets the format of the data to a stack
 * @head: Pointer to the stack's head
 * @counter: Line number
 * Return: No return value
*/

void stack_func(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}

